var Storage = (function(){var fetchDataFromStorage = function(storage,prefix){	var storageHolder=[];	var y = 0;		for (var i=0;i<storage.length;i++){			if (storage.getItem('lastID')!=null && storage.key(i).indexOf(prefix)!=-1){				storageHolder[y++] = storage.getItem(storage.key(i));			}		}	return storageHolder;};var fetchDataById = function(storage,id) {		return $.parseJSON(storage.getItem('personData'+id));};var initializeID = function() {  var lastID = localStorage.getItem('lastID');  if ( lastID == null ) {	lastID = 0;     localStorage.setItem( 'lastID',lastID);  } else {		localStorage.setItem( 'lastID',++lastID);  }  return lastID;};	return {		fetchDataFromStorage:fetchDataFromStorage,		fetchDataById:fetchDataById,		initializeID:initializeID	}})()